project_name: "long-context-comparison"
run_name: "longt5_qa"

task:
  name: "qa"
  data_path: "data/processed/pilot_qa.jsonl"
  max_samples: 5
  shuffle: false
  seed: 42

model:
  name: "google/long-t5-tglobal-base"
  architecture: "encoder-decoder"
  use_4bit: false

generation:
  max_input_tokens: 2048
  max_new_tokens: 32
  do_sample: false
  temperature: 0.0

runtime:
  device: "auto"

output:
  predictions_dir: "outputs/predictions"
  metrics_dir: "outputs/metrics"
  logs_dir: "outputs/logs"
